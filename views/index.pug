extends layout

block content
  +nav(project.projectname + ' Kreditportal', "dropdown_menu", project.logo) 
    +nav_item('/project') 
      span.fa.fa-th-large
      span  Projekt auswählen    
  .container-fluid(id="root-container" class="col-xl-8 col-xl-offset-2 col-lg-12")  
    .jumbotron
      h2= project.projectname + " @ habiDAT"
      p.lead Bitte melde dich mit deinen Zugangsdaten an. Die Daten wurden dir nach Vertragsabschluss per Post zugesandt oder persönlich übergeben. Falls du die Zugangsdaten nicht bekommen hast oder verloren hast, dann melde dich bitte unter #[a(href='mailto:#{project.email}') #{project.email}] 
    
    if message && message.length > 0
      div(class="alert alert-danger") 
        p.message= message

    .col-md-5.col-xs-12.col-sm-6.col-lg-4
      +logon_form("logon")
