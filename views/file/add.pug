include ../partials/forms
+form('/file/add/user')(updateTag='user_files' updateAction='replace')
  +hidden('id', id)
  +hidden('type', type)
  +file('file')(icon='fa-upload' required)
  +text('description')(icon='fa-comment-alt')
  +submit_button()