#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../json2csv/bin/json2csv.js" "$@"
  ret=$?
else 
  node  "$basedir/../json2csv/bin/json2csv.js" "$@"
  ret=$?
fi
exit $ret
